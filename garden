local UIS = game:GetService("UserInputService")

-- Safe check for gethui() with CoreGui fallback to prevent crashing
local targetParent = nil
local success, err = pcall(function()
    targetParent = gethui()
end)
if not success or not targetParent then
    targetParent = game:GetService("CoreGui")
end

-- Main ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "GardenScriptsGui"
gui.ResetOnSpawn = false
gui.Parent = targetParent

-- Main Frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 550, 0, 400)
frame.Position = UDim2.new(0.5, -275, 0.5, -200)
frame.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
frame.BorderSizePixel = 0

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 16)
frameCorner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(55, 55, 80)
stroke.Thickness = 1.5
stroke.Parent = frame

-- Drag Bar
local dragBar = Instance.new("Frame")
dragBar.Size = UDim2.new(1, 0, 0, 55)
dragBar.BackgroundColor3 = Color3.fromRGB(24, 24, 36)
dragBar.BorderSizePixel = 0

local dragBarCorner = Instance.new("UICorner")
dragBarCorner.CornerRadius = UDim.new(0, 16)
dragBarCorner.Parent = dragBar

-- Drag Bar Corner Fix
local dragBarFix = Instance.new("Frame")
dragBarFix.Size = UDim2.new(1, 0, 0.5, 0)
dragBarFix.Position = UDim2.new(0, 0, 0.5, 0)
dragBarFix.BackgroundColor3 = Color3.fromRGB(24, 24, 36)
dragBarFix.BorderSizePixel = 0
dragBarFix.Parent = dragBar

-- Title Text
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.6, 0, 1, 0)
title.Position = UDim2.new(0, 16, 0, 0)
title.BackgroundTransparency = 1
title.Text = "🌿Garden Scripts"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 17
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextYAlignment = Enum.TextYAlignment.Center
title.Parent = dragBar

-- Credit Text
local credit = Instance.new("TextLabel")
credit.Size = UDim2.new(0, 110, 1, 0)
credit.Position = UDim2.new(1, -155, 0, 0)
credit.BackgroundTransparency = 1
credit.Text = "gardendevv"
credit.TextColor3 = Color3.fromRGB(100, 100, 140)
credit.TextSize = 16
credit.Font = Enum.Font.Gotham
credit.TextXAlignment = Enum.TextXAlignment.Center
credit.TextYAlignment = Enum.TextYAlignment.Center
credit.Parent = dragBar

-- Close Button
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 32, 0, 32)
closeBtn.Position = UDim2.new(1, -44, 0.5, -16)
closeBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 13
closeBtn.Font = Enum.Font.GothamBold
closeBtn.BorderSizePixel = 0

local closeBtnCorner = Instance.new("UICorner")
closeBtnCorner.CornerRadius = UDim.new(0, 8)
closeBtnCorner.Parent = closeBtn
closeBtn.Parent = dragBar

dragBar.Parent = frame

-- Divider Line
local line = Instance.new("Frame")
line.Size = UDim2.new(1, -40, 0, 1)
line.Position = UDim2.new(0, 20, 0, 55)
line.BackgroundColor3 = Color3.fromRGB(50, 50, 75)
line.BorderSizePixel = 0
line.Parent = frame

-- Main Messages
local msg = Instance.new("TextLabel")
msg.Size = UDim2.new(1, -60, 0, 80)
msg.Position = UDim2.new(0, 30, 0.5, -60)
msg.BackgroundTransparency = 1
msg.Text = "You must join the Garden Scripts Roblox group to access."
msg.TextColor3 = Color3.fromRGB(220, 220, 240)
msg.TextSize = 22
msg.Font = Enum.Font.GothamBold
msg.TextWrapped = true
msg.TextXAlignment = Enum.TextXAlignment.Center
msg.Parent = frame

local subMsg = Instance.new("TextLabel")
subMsg.Size = UDim2.new(1, -60, 0, 30)
subMsg.Position = UDim2.new(0, 30, 0.5, -5)
subMsg.BackgroundTransparency = 1
subMsg.Text = "Click below to copy the group link - it only takes a second!"
subMsg.TextColor3 = Color3.fromRGB(130, 130, 170)
subMsg.TextSize = 14
subMsg.Font = Enum.Font.Gotham
subMsg.TextWrapped = true
subMsg.TextXAlignment = Enum.TextXAlignment.Center
subMsg.Parent = frame

-- Proceed Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 160, 0, 44)
button.Position = UDim2.new(0.5, -80, 1, -68)
button.BackgroundColor3 = Color3.fromRGB(70, 120, 240)
button.Text = "Proceed"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.TextSize = 16
button.Font = Enum.Font.GothamBold
button.BorderSizePixel = 0

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 10)
buttonCorner.Parent = button

local btnStroke = Instance.new("UIStroke")
btnStroke.Color = Color3.fromRGB(100, 150, 255)
btnStroke.Thickness = 1
btnStroke.Parent = button
button.Parent = frame

-- Resize Handle
local resizeHandle = Instance.new("TextButton")
resizeHandle.Size = UDim2.new(0, 18, 0, 18)
resizeHandle.Position = UDim2.new(1, -20, 1, -20)
resizeHandle.BackgroundColor3 = Color3.fromRGB(55, 55, 80)
resizeHandle.Text = ""
resizeHandle.BorderSizePixel = 0

local resizeCorner = Instance.new("UICorner")
resizeCorner.CornerRadius = UDim.new(0, 4)
resizeCorner.Parent = resizeHandle
resizeHandle.Parent = frame

-- Open Button (Minimized State)
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0, 150, 0, 38)
openBtn.Position = UDim2.new(0, 20, 0, 20)
openBtn.BackgroundColor3 = Color3.fromRGB(70, 120, 240)
openBtn.Text = "🌿Garden Scripts"
openBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
openBtn.TextSize = 14
openBtn.Font = Enum.Font.GothamBold
openBtn.BorderSizePixel = 0
openBtn.Visible = false

local openBtnCorner = Instance.new("UICorner")
openBtnCorner.CornerRadius = UDim.new(0, 8)
openBtnCorner.Parent = openBtn
openBtn.Parent = gui

-- Finally, parent the interface container to the game window
frame.Parent = gui

-- Interactivity Logic
closeBtn.MouseButton1Click:Connect(function()
    frame.Visible = false
    openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
    frame.Visible = true
    openBtn.Visible = false
end)

button.MouseButton1Click:Connect(function()
    if setclipboard then
        pcall(function() setclipboard("https://short-url.cc/gardendevv") end)
    end
    button.Text = "Link Copied!"
    button.BackgroundColor3 = Color3.fromRGB(50, 160, 90)
    task.delay(2, function()
        button.Text = "Proceed"
        button.BackgroundColor3 = Color3.fromRGB(70, 120, 240)
    end)
end)

local dragging, dragStart, startPos
local resizing, resizeStart, startSize

dragBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
    end
end)

dragBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

resizeHandle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        resizing = true
        resizeStart = input.Position
        startSize = frame.Size
    end
end)

resizeHandle.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        resizing = false
    end
end)

UIS.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        if dragging and dragStart and startPos then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        elseif resizing and resizeStart and startSize then
            local delta = input.Position - resizeStart
            local newW = math.max(250, startSize.X.Offset + delta.X)
            local newH = math.max(150, startSize.Y.Offset + delta.Y)
            frame.Size = UDim2.new(0, newW, 0, newH)
        end
    end
end)
